from distutils.core import setup

setup(
    name='hosts',
    version='0.0.1',
    description='Manage /etc/hosts without buggering it up',
    author='Peter Sutton',
    author_email='foxxy@foxdogstudios.com',
    packages=['hosts'],
)
